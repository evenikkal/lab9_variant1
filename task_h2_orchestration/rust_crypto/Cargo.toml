[package]
name = "rustcrypto"
version = "0.1.0"
edition = "2021"

[lib]
name = "rustcrypto"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }